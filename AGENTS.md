# FOSU working guidance

For parser changes and performance experiments, follow
[docs/validation.md](docs/validation.md). Use the fixed routine profiles first;
do not run the full corpus/machine/interface matrix for every hypothesis.
Keep compatibility coverage separate from the play-representative timing
workload. Report per-mode results and never label the equal-mode average as
observed player traffic. Keep baseline/candidate artifacts immutable while used.
